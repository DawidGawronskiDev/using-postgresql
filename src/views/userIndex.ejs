<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Users</title>
  </head>
  <body>
    <h1>Users Page!</h1>
    <ul>
      <% users.forEach(item => { %>
      <li><%= item %></li>
      <!-- Output each item in the list -->
      <% }); %>
    </ul>
  </body>
</html>
